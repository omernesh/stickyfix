---
phase: 05-element-note-mode-rich-context-capture
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - entrypoints/review.content/picker.ts
  - entrypoints/review.content/chip.ts
  - entrypoints/review.content/styles.css
autonomous: false
requirements: [ELEM-01]
user_setup: []

must_haves:
  truths:
    - "Clicking the 🎯 picker button enters pick mode: the picker button shows its active state and the page cursor becomes crosshair"
    - "Hovering a page element draws an orange outline + a cursor-following 'tag · WxH' pill at the element's rect"
    - "Esc exits pick mode cleanly with no card and resets the picker button to resting"
    - "Clicking the 🎯 button again while active toggles pick mode off (no card)"
    - "Clicking a page element exits pick mode and invokes the onElementClick callback with the clicked element (card wiring lands in Plan 03)"
    - "The hover overlay never reacts to sfx's own shadow-root elements (guarded) and is pointer-events:none so the page still receives the click"
  artifacts:
    - path: "entrypoints/review.content/picker.ts"
      provides: "enterPickMode / exitPickMode pick-mode lifecycle + hover overlay"
      contains: "export function enterPickMode"
      min_lines: 70
    - path: "entrypoints/review.content/chip.ts"
      provides: "🎯 picker button + active-state toggle wired to enter/exitPickMode"
      contains: "sfx-picker-btn"
    - path: "entrypoints/review.content/styles.css"
      provides: ".sfx-picker-btn, .sfx-hover-highlight, .sfx-hover-label, :host.sfx-pick-mode cursor"
      contains: "sfx-hover-highlight"
  key_links:
    - from: "entrypoints/review.content/chip.ts"
      to: "entrypoints/review.content/picker.ts"
      via: "picker button click → enterPickMode / exitPickMode"
      pattern: "enterPickMode|exitPickMode"
    - from: "entrypoints/review.content/picker.ts"
      to: "document"
      via: "mousemove + click + keydown listeners with stored cleanup"
      pattern: "addEventListener"
---

<objective>
Deliver the pick-mode half of Element-Note Mode: a 🎯 picker button on the connection chip that toggles a document-level hover-highlight overlay (orange outline + cursor-following `tag · WxH` pill), with Esc / re-click cancel and a single-shot click that hands the clicked element to a callback. This is the first user-visible slice — after this plan a developer can enter Review Mode, click 🎯, and visually inspect any element on the page; the click-to-card wiring lands in Plan 03.

Purpose: ELEM-01 (picker enters pick mode; hover highlights with outline + `tag · WxH`; Esc cancels). Keeps `card.ts` clean by isolating pick lifecycle in a new `picker.ts` (per UI-SPEC Component Extension Map).
Output: `entrypoints/review.content/picker.ts` (new); 🎯 button + toggle in `chip.ts`; picker/hover/cursor CSS in `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-element-note-mode-rich-context-capture/05-RESEARCH.md
@.planning/phases/05-element-note-mode-rich-context-capture/05-PATTERNS.md
@.planning/phases/05-element-note-mode-rich-context-capture/05-UI-SPEC.md

<interfaces>
<!-- Contract picker.ts exposes; chip.ts + (Plan 03) index.ts consume it. -->
```typescript
// picker.ts public API
export function enterPickMode(
  container: HTMLElement,
  onElementClick: (el: Element) => void,
  onEsc: () => void
): void;
export function exitPickMode(): void;
```

<!-- chip.ts current button-construction analog (lines ~133-144): -->
```typescript
const sendBtn = document.createElement('button');
sendBtn.className = 'sfx-chip-btn sfx-btn-send';
sendBtn.textContent = 'Send';
sendBtn.setAttribute('aria-label', 'Send stub annotation (relay proof)');
chip.appendChild(sendBtn);
```
<!-- chip teardownMap (WeakMap<HTMLElement, ()=>void>) registers chip removal; extend to also exitPickMode. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: picker.ts — pick-mode lifecycle + hover overlay</name>
  <files>entrypoints/review.content/picker.ts, entrypoints/review.content/styles.css</files>
  <read_first>
    - entrypoints/review.content/fab.ts (analog — module-level mutable state pattern; createElement/textContent DOM build; addEventListener lifecycle; no chrome.* calls of its own)
    - entrypoints/review.content/chip.ts lines ~444-457 (event-target guard: `target.closest(...)` skip pattern — picker mirrors with `container.contains(target)` skip)
    - entrypoints/review.content/styles.css (the file being modified — existing tokens, :host cursor default, .sfx-chip-btn:hover) 
    - lib/capture.ts waitTwoRafs (rAF usage analog for the mousemove rAF throttle)
    - 05-RESEARCH.md Pattern 3 (hover overlay lifecycle: enterPickMode/updateOverlay/onMouseMove rAF throttle/onClick/onKeyDown + cleanup refs) + Pitfall 5 (skip shadow-host targets)
    - 05-PATTERNS.md "picker.ts" section (state vars, rAF throttle, shadow-host guard, DOM-via-textContent)
    - 05-UI-SPEC.md §2 Hover-Highlight Overlay (positioning fixed at rect, orange border rgba(255,140,0,0.85), fill rgba(255,140,0,0.08), label pill rgba(0,0,0,0.75)/#fff, 4px 8px pad, near-bottom flip, max-width 240) + Pick Mode State Machine + Keyboard Contract + label format `tag · WxH` / `tag#id` / `tag.class` ≤20 char prefix, Math.round CSS-px dims (NOT dpr-scaled)
  </read_first>
  <action>
    Create `entrypoints/review.content/picker.ts` with no chrome.* calls (pure DOM/event module). Module-level state: `hoverOverlay: HTMLDivElement|null`, `hoverLabel: HTMLSpanElement|null`, `currentTarget: Element|null`, `_cleanupFns: Array<()=>void>`, and a `_container` ref. Export `enterPickMode(container, onElementClick, onEsc)` and `exitPickMode()`. enterPickMode: build the overlay div `.sfx-hover-highlight` + label span `.sfx-hover-label` via createElement (label text via `textContent` only — INVARIANT C), append to container, add `sfx-pick-mode` class to the shadow `:host` (toggle the host element's classList so the crosshair cursor rule applies), and register `document.addEventListener` for `mousemove` (rAF-throttled via a single `rafPending` flag), `click`, and `keydown`. mousemove: skip when `e.target === container || container.contains(target)` (Pitfall 5) and when `target === currentTarget`; else updateOverlay(target). updateOverlay: position the overlay at `getBoundingClientRect()` (position:fixed left/top/width/height in px), set label `textContent` to the prefix (tag / tag#id / tag.class, ≤20 chars) + ` · ${Math.round(width)}×${Math.round(height)}`, flip the label above/below using the near-bottom (within 24px of innerHeight) rule. click: capture `e.target`, call `exitPickMode()` THEN `onElementClick(target)` — do NOT call preventDefault/stopPropagation (UI-SPEC: page handles its own click; accept as v1). keydown: on `Escape` call `exitPickMode()` then `onEsc()`. exitPickMode: run all `_cleanupFns` (removeEventListener), remove the overlay from the DOM, remove `sfx-pick-mode` from `:host`, reset `currentTarget`/`hoverOverlay`/`hoverLabel` to null. Make exitPickMode idempotent (safe to call when not active). Then in `styles.css` add (px units only — never rem): `.sfx-hover-highlight` (position:fixed, box-sizing:border-box, pointer-events:none, z-index:1, border:2px solid rgba(255,140,0,0.85), background:rgba(255,140,0,0.08), border-radius:2px, display:none default), `.sfx-hover-label` (position:absolute, left:0, bottom:calc(100% + 4px), background:rgba(0,0,0,0.75), color:#ffffff, font-size:13px, font-weight:400, line-height:1, padding:4px 8px, border-radius:3px, white-space:nowrap, pointer-events:none, max-width:240px, overflow:hidden, text-overflow:ellipsis), and `:host(.sfx-pick-mode) { cursor: crosshair; }`. Use UI-SPEC color tokens exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `entrypoints/review.content/picker.ts` exports `enterPickMode` and `exitPickMode` with the signatures above.
    - picker.ts contains no `chrome.` reference and no `innerHTML` (`grep -c "chrome\." entrypoints/review.content/picker.ts` == 0; `grep -c innerHTML entrypoints/review.content/picker.ts` == 0).
    - picker.ts mousemove handler contains a `container.contains` guard and a `rafPending` rAF throttle.
    - styles.css contains `.sfx-hover-highlight`, `.sfx-hover-label`, and a `sfx-pick-mode` crosshair cursor rule; orange tokens rgba(255,140,0,0.85) and rgba(255,140,0,0.08) present.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>picker.ts compiles, exposes enterPickMode/exitPickMode, builds a pointer-events:none hover overlay via textContent, throttles mousemove to rAF, guards sfx-internal targets, and exits cleanly on Esc/click; hover/label/cursor CSS added per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: chip.ts — 🎯 picker button + active-state toggle</name>
  <files>entrypoints/review.content/chip.ts, entrypoints/review.content/styles.css</files>
  <read_first>
    - entrypoints/review.content/chip.ts (the file being modified — sendBtn construction ~133-144, teardownMap WeakMap ~68-72 + 209-215, getTabId export ~527-542, existing chip flex layout/gap)
    - entrypoints/review.content/picker.ts (just created — enterPickMode/exitPickMode imports)
    - entrypoints/review.content/styles.css (the file being modified — .sfx-chip-btn base + :hover added in Task 1)
    - 05-PATTERNS.md "chip.ts" section (button construction mirror, teardown extension, getTabId pattern)
    - 05-UI-SPEC.md §1 Picker Button (placement after route label / before exit button; resting 28px round #f0f0f0 border #cccccc font-size 16px; active bg #fff3e0 border #ff6b00 box-shadow glow; aria-label resting "Pick element" / active "Cancel element pick (Esc)"; aria-pressed false/true; base class .sfx-chip-btn + .sfx-picker-btn modifier, .sfx-active for active state) + Focus Management (on Esc picker btn receives focus)
  </read_first>
  <action>
    Extend `mountChip` in `chip.ts` to construct a picker `<button>`: `.sfx-chip-btn sfx-picker-btn`, `textContent = '🎯'`, `aria-label='Pick element'`, `aria-pressed='false'`, appended into `#sfx-chip` after the route label and before the exit button. mountChip must accept a new callback parameter (e.g. `onPickerClick?: () => void`) OR expose a setter — keep the existing 2-arg `mountChip(container, onExit)` call working; the picker wiring is supplied by index.ts in Plan 03. For THIS plan, wire the button's own toggle locally: on click, flip an `active` flag — when activating set `aria-pressed='true'`, add `.sfx-active`, and call `enterPickMode(container, onElementClick, onEsc)` where `onElementClick`/`onEsc` come from the injected callback (default no-op for now so the button is functional in isolation and fully wired in Plan 03); when deactivating (toggle or via onEsc/onElementClick) set `aria-pressed='false'`, remove `.sfx-active`, call `exitPickMode()`, and `pickerBtn.focus()` on Esc-driven exit (UI-SPEC Focus Management). Import `enterPickMode, exitPickMode` from `./picker.js`. Extend the chip's `teardownMap` entry to also call `exitPickMode()` when the chip is torn down (so pick mode never outlives the UI). Add the picker-button CSS to `styles.css`: `.sfx-picker-btn` (width/height 28px, border-radius 50%, background #f0f0f0, border 1px solid #cccccc, cursor pointer, font-size 16px, flex centering), `.sfx-picker-btn:hover` (#e0e0e0), `.sfx-picker-btn.sfx-active` (background #fff3e0, border-color #ff6b00, box-shadow 0 0 0 2px rgba(255,107,0,0.25)). px units only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - chip.ts constructs a button with class containing `sfx-picker-btn`, textContent `🎯`, `aria-pressed` attribute, and imports `enterPickMode`/`exitPickMode` from `./picker.js`.
    - The existing `mountChip(container, () => ui.remove())` 2-arg call site still type-checks (new picker param is optional or a setter).
    - chip teardown calls `exitPickMode()`.
    - styles.css contains `.sfx-picker-btn`, `.sfx-picker-btn:hover`, and `.sfx-picker-btn.sfx-active` with bg #fff3e0 / border #ff6b00.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The chip renders a 🎯 button that toggles pick mode (active visual + aria-pressed), enters/exits picker.ts cleanly, restores focus to the button on Esc exit, and tears down pick mode with the chip; CSS matches UI-SPEC §1. The element-click callback defaults to no-op (fully wired in Plan 03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The 🎯 picker button on the connection chip and the document-level hover-highlight overlay (orange outline + cursor-following `tag · WxH` pill), with Esc / re-click cancel. Build the extension (`npm run build`) and load `.output/chrome-mv3` (or run `npm run dev`).</what-built>
  <how-to-verify>
    1. Load the built extension in Chrome (chrome://extensions → Load unpacked → the WXT output dir).
    2. Open any content-rich HTTPS page (e.g. a news site or a React app). Open the stickyfix popup and enter Review Mode so the connection chip appears top-right.
    3. Click the 🎯 button on the chip. Confirm: the button shows the active orange state (light-orange bg, orange border, glow ring) and the page cursor becomes a crosshair.
    4. Move the mouse over different elements. Confirm: an orange outline tracks the element under the cursor, and a dark pill label shows e.g. `button · 120×40` (or `div#main · WxH` / `a.nav-link · WxH`), following the cursor; the label flips above the box near the bottom edge.
    5. Press Esc. Confirm: the overlay disappears, the cursor returns to normal, the 🎯 button returns to resting state, and NO card opens. Confirm keyboard focus is on the 🎯 button.
    6. Click 🎯 again to re-enter, then click 🎯 once more (toggle). Confirm pick mode turns off with no card.
    7. While in pick mode, click a page element. Confirm pick mode exits (no card yet — the click→card wiring is Plan 03; the element click should be a clean exit with no console error).
    8. Open the page's devtools console. Confirm no stickyfix errors during hover/Esc/click.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (cursor, outline tracking, label format/position, active-state visuals, Esc behavior, focus).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → content script | mousemove/click `e.target` is a page-controlled element; its tag/id/class strings are untrusted |
| content script ↔ shadow-root UI | overlay/label live in the shadow root; must never be the picker target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (XSS) | hover label text | mitigate | Label prefix (tag/id/class) set via `textContent` only — never innerHTML. Enforced by grep gate in Task 1 acceptance. |
| T-05-06 | Tampering | finder called on shadow element | mitigate | mousemove guards `container.contains(target)` so sfx-internal elements are never picked (Pitfall 5); finder itself runs in Plan 03 on page elements only. |
| T-05-07 | Spoofing | page forges picker click | accept | `e.target` is set by the browser's event dispatch; a page script cannot forge an arbitrary target to a real document click (RESEARCH security domain). Low risk, developer-only tool. |
| T-05-08 | — | pick mode click also hits page handler | accept | UI-SPEC: not calling stopPropagation is intentional for v1; page may handle its own click. Documented; aggressive-capture pages are a Phase 8 edge case (RESEARCH Open Q3). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (extension half type-checks).
- No `innerHTML` in picker.ts; no `chrome.` in picker.ts.
- Manual Chrome UAT (checkpoint) confirms ELEM-01: hover outline + `tag · WxH` label following cursor, Esc cancels, toggle cancels, crosshair cursor, focus restore — Success Criterion 1.
</verification>

<success_criteria>
- picker.ts exposes enterPickMode/exitPickMode and renders a pointer-events:none hover overlay with a textContent label.
- chip.ts renders a 🎯 toggle button with correct active visuals + aria-pressed, wired to picker enter/exit, with focus restore on Esc and teardown integration.
- styles.css carries the picker/hover/cursor tokens exactly per UI-SPEC §1/§2.
- ELEM-01 manually verified in Chrome (Success Criterion 1).
</success_criteria>

<output>
Create `.planning/phases/05-element-note-mode-rich-context-capture/05-02-SUMMARY.md` when done
</output>
